apiVersion: v1
kind: Service
metadata:
  name: {{ include "mysql-57.fullname" . }}
  labels:
    {{- include "mysql-57.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mysql-57.selectorLabels" . | nindent 4 }}
