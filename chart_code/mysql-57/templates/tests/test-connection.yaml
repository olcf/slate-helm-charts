apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mysql-57.fullname" . }}-test-connection"
  labels:
    {{- include "mysql-57.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mysql-57.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
